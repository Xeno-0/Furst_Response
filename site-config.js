window.FurstResponseConfig = {
  // If you deploy the FRBackend server separately, put its full URL here.
  // If the backend serves this site from the same domain, leave this blank.
  // Example: "https://your-backend.onrender.com"
  backendBaseUrl: "",

  // Request timeout in milliseconds.
  requestTimeoutMs: 30000,
};
